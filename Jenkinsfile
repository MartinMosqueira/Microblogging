pipeline {
    agent any  // Ejecutar en cualquier nodo disponible

    stages {
        stage('Test') {
            steps {
                // Imprime un mensaje para verificar que el pipeline se está ejecutando
                echo '¡El Jenkinsfile está funcionando!'
            }
        }
    }
}
